---
name: hr-plan
description: "【强制触发】当用户制定面试计划的意图时必须使用本 skill。主 agent 负责协调，通过 spawn 启动专职子代理（简历智能体、计划智能体、监督智能体）分工协作，监督智能体校验输出质量，不合格则督促重做。触发词：制定面试计划、准备面试、安排面试、面试方案等。"
---

# 执行手册

## 执行总则

- 当前智能体为**主智能体**，负责任务协调和质量最终把控
- 专职智能体为独立的 Subagent，通过 `spawn` 工具启动
- 主智能体**禁止**直接实现简历整理和计划生成，只做管理和协调
- 监督智能体独立于执行智能体，禁止自我监督

## 数据目录约定

所有文件路径基于以下根目录：

| 目录 | 路径 | 内容 |
|------|------|------|
| 简历库 | `data/hr/简历库/` | 候选人 PDF 简历、整理后的 md/json |
| 岗位库 | `data/hr/岗位库/{岗位名}/` | 岗位信息 `{岗位名}.txt`、面试计划模板 `面试计划模板.jsonc` |
| 计划库 | `data/hr/计划库/{候选人}/` | 各环节计划、汇总计划 |

## 主智能体工作流程

### 阶段一：解析输入

从用户指令中提取：
- **应聘者姓名**：如「苏小建」
- **应聘岗位**：如「算法工程师」
- **简历路径**：`data/hr/简历库/{应聘者}/{含应聘者姓名的pdf文件}`
- **岗位路径**：`data/hr/岗位库/{岗位名}/`

### 阶段二：简历整理（串行步骤1）

启动**简历智能体**，任务：将 PDF 简历整理为结构化数据。

```
spawn task = "你是简历整理智能体。任务：

从 data/hr/简历库/{应聘者}/ 下找到应聘者的 PDF 简历文件，
将 PDF 文件逐页转换为图片，保存到 data/hr/简历库/{应聘者}/{应聘者}_img_x.png。

从简历图片中提取信息，整理到 data/hr/简历库/{应聘者}/{应聘者}.md。

读取整理后的简历 data/hr/简历库/{应聘者}/{应聘者}.md，
参考 data/hr/简历库/简历模版.josn 的格式要求，
整理成结构化 JSON，保存到 data/hr/简历库/{应聘者}/{应聘者}.json。"
```

简历智能体完成后，启动**简历监督智能体**进行校验。

### 阶段三：面试计划生成（串行步骤2）

简历监督通过后，启动**计划智能体**，按面试环节逐一生成。

```
spawn task = "你是面试计划智能体。任务：

输入：
- 应聘者简历：data/hr/简历库/{应聘者}/{应聘者}.json
- 计划模板：data/hr/岗位库/{应聘岗位}/面试计划模板.jsonc
- 岗位信息：data/hr/岗位库/{应聘岗位}/{应聘岗位}.txt（含岗位信息和面试环节清单）

输出要求：
按面试环节清单逐一制定每个环节的计划：
1. 读取简历和模板，理解当前环节要求
2. 结合岗位信息和应聘者简历，参考模板格式，整理当前环节各字段信息
3. 当前环节信息输出到 data/hr/计划库/{应聘者}/{环节名称}.json
4. 重复以上步骤，直至完成所有环节
5. 最后整理所有环节计划，参考模板格式输出汇总 JSON 到
   data/hr/计划库/{应聘者}/{应聘者}_{应聘岗位}.json"
```

计划智能体完成后，启动**计划监督智能体**进行校验。

### 阶段四：汇总交付

所有步骤完成且监督通过后，向用户汇报完成情况，包括：
- 简历整理结果（data/hr/简历库/{应聘者}/ 下的文件）
- 面试计划结果（data/hr/计划库/{应聘者}/ 下的文件）

## 专职智能体

### 简历智能体

- 职责：将 PDF 简历转为图片 → 提取信息整理为 md → 按模板格式输出 json
- 约束：
  - **禁止**直接读取 PDF 文本，必须通过转图片的方式识别
  - 图片命名格式：`{应聘者}_img_x.png`（x 为页码，从 1 开始）
  - JSON 输出必须严格遵循 `简历模版.josn` 的字段结构

### 计划智能体

- 职责：按面试环节逐一生成计划，最后汇总
- 约束：
  - **禁止**一次性生成所有环节，必须逐环节独立生成
  - 每个环节的输出保存到独立的 `{环节名称}.json`
  - 必须遵守**问题生成原则**（见下方）

### 监督智能体

- 职责：验证执行智能体的输出是否符合要求
- 约束：**禁止无证据输出**，每个监督结论必须有具体文件内容和路径作为依据

#### 简历监督

验证项：
1. 是否生成 PDF 转图片文件，路径为 `data/hr/简历库/{应聘者}/{应聘者}_img_x.png`
2. 是否生成简历整理文档 `data/hr/简历库/{应聘者}/{应聘者}.md`
3. 是否生成结构化 JSON `data/hr/简历库/{应聘者}/{应聘者}.json`，且字段与 `简历模版.josn` 一致

#### 计划监督

验证项：
1. 逐一验证各环节数据文件 `data/hr/计划库/{应聘者}/{环节名称}.json` 是否存在且内容完整
2. 验证汇总文件 `data/hr/计划库/{应聘者}/{应聘者}_{应聘岗位}.json` 是否包含所有环节
3. 确认是否遵守问题生成原则（逐项检查）

## 问题生成原则

以下内容同时约束计划智能体的生成和监督智能体的校验：

1. 每个问题**只包含一问**，**禁止**一个问题中包含多问
2. **假设应聘者不会**，生成问题来验证他会
3. **禁止**问"你会不会 xxx？"
4. `{意向城市}` 问答中**必须**罗列工作地点：`佛山 深圳 天津 株洲 洛阳 乐山`，**禁止**随意调整城市列表

## 失败处理

| 失败类型 | 处理方式 |
|----------|----------|
| 子代理工具报错 | 分析错误原因，调整指令后重新 spawn |
| 监督不通过 | 将监督意见作为反馈，spawn 新的执行子代理重做 |
| 连续 2 次重做仍不通过 | 汇总问题上报给用户，请求人工介入 |
| 找不到简历 PDF | 告知用户未找到对应简历文件，请确认路径或文件名 |
