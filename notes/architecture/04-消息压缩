1. 需要移除的 token 数         [尽量达到]
2. 用户消息边界（保证完整性）
3. 最多 60 条消息上限
优先级顺序

条件 2（用户消息边界） → 硬性要求，必须满足
条件 3（60 条上限）    → 硬性上限，不能超过
条件 1（移除 token 数） → 目标，尽量达到

实际执行顺序

# Step 1: 用条件 1 + 条件 2 找理论边界
boundary = self.pick_consolidation_boundary(
    session,
    estimated - target  # 条件 1：目标移除量
)
# → 返回达到目标移除量的最近用户消息边界（条件 2）

# Step 2: 检查条件 3（60 条上限）
end_idx = self._cap_consolidation_boundary(session, boundary[0])
# → 如果超过 60 条，强制截断并回退到用户边界

三种情况

情况 A：正常情况（三者都满足）

需移除 80K tokens → 扫描到第 35 条（用户消息）达到 → 35 < 60
→ 截取 35 条，三条件都满足 ✓

情况 B：消息很短，需要很多条才能达到目标

需移除 80K tokens → 扫描到第 80 条才达到 → 80 > 60
→ 强制截断到第 58 条（最近的用户消息）
→ 条件 1 未完全满足，但条件 2、3 满足
→ 多轮循环来逐步达到目标

情况 C：消息很长，少量消息就超过目标

需移除 80K tokens → 扫描到第 10 条就达到 → 10 < 60
需移除 80K tokens → 扫描到第 10 条就达到 → 10 < 60
→ 截取 10 条，三条件都满足 ✓
需移除 80K tokens → 扫描到第 10 条就达到 → 10 < 60
→ 截取 10 条，三条件都满足 ✓

图示

                    条件 2（必须）
                    用户消息边界
                        │
    条件 3（上限）       │       条件 1（目标）
    最多 60 条          │      移除足够 token
        │               │           │
        └───────────────┼───────────┘
                        │
                    最终截断位置
                        │
        ┌──────────────┴──────────────┐
        │  min(理论边界, 60条上限)      │
        │  且必须在用户消息处           │
        └─────────────────────────────┘

---
总结：
- 条件 2（用户边界）必须满足
- 条件 3（60 条上限）不能超过
- 条件 1（移除量）是目标，通过多轮循环逐步逼近